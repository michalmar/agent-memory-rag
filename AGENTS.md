# Agent governance

This file is **small on purpose**. It sets the rules an AI agent must follow
when working in this repo. Substantive workflow content belongs in skill
files, not here.

## CLI integration

- **ALWAYS** run terminal with setup ENV vars:
  - AZURE_CONFIG_DIR="$HOME/.azure-365"
  - COPILOT_HOME="$HOME/.copilot

